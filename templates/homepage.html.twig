{# Opnemen van de "Main" file #}
{% extends "main.html.twig" %}

{% block header %}
<img src="assets/img/logo-v2.png"/>
{% endblock %}


{# nu de block content op de main pagina vullen #}
{% block content %}
    {% for record in data %}
        <div class="col-5 border gerecht_blok">
            <img src="{{ record.gerecht.afbeelding }}" style="width: 300px; height: 190px;">
            <h1>{{ record.gerecht.titel }}</h1> <br>
            {{ record.gerecht.korte_omschrijving}} <br>
            {{ record.waardering}} <br>
            {{ record.prijs}} <br>
            {{ record.gemiddelde_waardering}} <br>
            {{ record.calorieën }} <br>
            <button>Smullen!</button>
        </div>
    {% endfor %}
     {# { Dump #}
       {# <pre>
        {{ dump(data) }}
       </pre> #}
{% endblock %}

 